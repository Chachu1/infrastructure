---
# Caddy role - reverse proxy with automatic HTTPS

- name: Install Caddy dependencies
  apt:
    name:
      - debian-keyring
      - debian-archive-keyring
      - apt-transport-https
      - curl
    state: present

- name: Add Caddy GPG key
  apt_key:
    url: https://dl.cloudsmith.io/public/caddy/stable/gpg.key
    state: present

- name: Add Caddy repository
  apt_repository:
    repo: "deb https://dl.cloudsmith.io/public/caddy/stable/deb/debian any-version main"
    state: present
    filename: caddy-stable

- name: Install Caddy
  apt:
    name: caddy
    state: present
    update_cache: yes

- name: Create Caddy systemd drop-in directory
  file:
    path: /etc/systemd/system/caddy.service.d
    state: directory
    mode: "0755"

- name: Set Cloudflare API token for Caddy DNS challenge
  copy:
    dest: /etc/systemd/system/caddy.service.d/cloudflare.conf
    content: |
      [Service]
      Environment=CLOUDFLARE_API_TOKEN={{ lookup('env', 'CLOUDFLARE_API_TOKEN') }}
    mode: "0644"
  notify:
    - reload systemd
    - restart caddy

- name: Deploy Caddyfile
  template:
    src: Caddyfile.j2
    dest: /etc/caddy/Caddyfile
    backup: yes
  notify: restart caddy

- name: Ensure Caddy is running
  systemd:
    name: caddy
    state: started
    enabled: yes
