# Managed by Ansible
# Caddy reverse proxy configuration

{
    email admin@{{ domain }}
}

{% for host in caddy_hosts %}
{{ host.domain }} {
    reverse_proxy {{ host.backend }}:{{ host.port }}
}
{% endfor %}
