# Managed by Ansible
# Caddy reverse proxy configuration
# v3 - DNS challenge for Cloudflare

{
    email admin@{{ domain }}
    acme_dns cloudflare
}

{% for host in caddy_hosts %}
{{ host.domain }} {
    reverse_proxy {{ host.backend }}:{{ host.port }}
}
{% endfor %}
