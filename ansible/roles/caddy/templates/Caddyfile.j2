# Managed by Ansible
# Caddy reverse proxy configuration
# v2 - proxy entries managed via gateway_services.yml

{
    email admin@{{ domain }}
}

{% for host in caddy_hosts %}
{{ host.domain }} {
    reverse_proxy {{ host.backend }}:{{ host.port }}
}
{% endfor %}
